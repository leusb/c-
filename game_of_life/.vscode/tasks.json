{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "cmake -S . -B build && cmake --build build && ./build/game_of_life",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "CMake-Projekt bauen und ausführen"
        }
    ]
}